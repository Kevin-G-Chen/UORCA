# UORCA Explorer Environment Configuration
# Copy this file to .env and fill in your actual values

# Required for full AI landing page features
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Default directory for UORCA results
# If not set, the app will look for UORCA_results in common locations
UORCA_DEFAULT_RESULTS_DIR=/path/to/your/UORCA_results

# Optional: OpenAI model to use for AI features
# Options: gpt-4o-mini (default, fast & cost-effective), gpt-4o (more powerful)
OPENAI_MODEL=gpt-4o-mini

# Optional: Set log level for debugging
# Options: DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO