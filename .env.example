# UORCA Environment Configuration Template
# ========================================
# Copy this file to '.env' and fill in your actual values
# Keep this file secure and never commit it to version control!

# Required: Your email address (NCBI Entrez API requirement)
# Any valid email address - required by NCBI guidelines for API access
# Example: researcher@university.edu
ENTREZ_EMAIL=your.email@institution.edu

# Required: OpenAI API key for AI-powered analysis
# Get your key from: https://platform.openai.com/api-keys
# Format: sk-proj-... (for newer keys) or sk-... (for older keys)
# Used for: Dataset relevance scoring, AI analysis features
OPENAI_API_KEY=sk-proj-your-actual-openai-api-key-here

# Optional but recommended: NCBI API key for faster processing
# Get your FREE key from: https://www.ncbi.nlm.nih.gov/account/settings/
# Benefits: 10x faster database queries (10 req/sec vs 3 req/sec)
# Format: alphanumeric string
# ENTREZ_API_KEY=your-ncbi-api-key-here

# ========================================
# Additional Configuration (Advanced Users)
# ========================================

# Optional: Override default OpenAI model for dataset identification
# Default: gpt-4o-mini (cost-effective)
# Alternatives: gpt-4o, gpt-4-turbo
# OPENAI_MODEL=gpt-4o-mini

# Optional: Custom rate limiting (experts only)
# ENTREZ_RATE_LIMIT=3.0  # requests per second without API key
# ENTREZ_RATE_LIMIT_WITH_KEY=10.0  # requests per second with API key

# ========================================
# Setup Instructions
# ========================================
# 1. Copy this template: cp .env.template .env
# 2. Edit .env with your actual values
# 3. Test your setup: python validate_environment.py
# 4. Never commit .env to version control (it's in .gitignore)
#
# For detailed setup help, see: ENVIRONMENT_SETUP.md
# For troubleshooting, run: python validate_environment.py
